{
  "name": "d10",
  "version": "1.0.0",
  "description": "Roll a single ten-sided die, beautifully.",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "bun ./src/index.html",
    "build": "bun run build.ts",
    "preview": "bun run build && bun ./scripts/serve-dist.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/html-minifier-terser": "^7.0.2",
    "html-minifier-terser": "^7.2.0"
  }
}
